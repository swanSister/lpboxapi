CREATE TABLE lp_list (
lpId VARCHAR(255) NOT NULL PRIMARY KEY,
userId VARCHAR(255),
name VARCHAR(255),
singer VARCHAR(255),
releaseDate VARCHAR(255),
description VARCHAR(255),
price VARCHAR(255),
genre VARCHAR(255),
imgList JSON,
createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ,
updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE user_list (
userId VARCHAR(255) NOT NULL PRIMARY KEY,
id VARCHAR(255),
pw VARCHAR(255),
createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ,
updatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

INSERT INTO user_list VALUES ('S1234', 'KJK', '0000', UTC_TIMESTAMP(), UTC_TIMESTAMP());
INSERT INTO user_list VALUES ('S4321', 'JYJ', '0000', UTC_TIMESTAMP(), UTC_TIMESTAMP());